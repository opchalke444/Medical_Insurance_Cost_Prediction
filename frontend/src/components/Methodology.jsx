import React, { useState } from 'react';
import { HelpCircle, ChevronDown, ChevronUp, BookOpen, AlertCircle, Database } from 'lucide-react';

const PROJECT_BOUNDARIES = [
  {
    icon: BookOpen,
    title: "Educational & Informational Scope",
    desc: "This project was developed as an academic machine learning demonstration. It is intended strictly for educational exploration and personal budgeting awareness."
  },
  {
    icon: AlertCircle,
    title: "Dataset & Population Limitations",
    desc: "This model learns patterns from its training dataset. Differences in the dataset may affect predictions, and model performance on other populations may differ."
  },
  {
    icon: Database,
    title: "In-Memory Input Handling",
    desc: "The application does not intentionally store prediction inputs in a database. Inputs sent to the server are processed in memory to return the regression estimate."
  }
];

const FAQS = [
  {
    q: "1. How is the estimate calculated?",
    a: "The estimate is calculated using a K-Nearest Neighbors (KNN) regression model trained on 1,338 historical medical insurance cost records. Numerical inputs (age, BMI, children) are standardized, categorical features (sex, smoker, region) are one-hot encoded, and the model estimates annual charges based on the average of the 9 closest records in the dataset."
  },
  {
    q: "2. What inputs are used?",
    a: "The model requires six inputs: age (years), biological sex (male or female as recorded in the dataset), Body Mass Index (BMI in kg/m²), number of dependents (children), smoking status (yes or no), and US residential region (northeast, northwest, southeast, southwest)."
  },
  {
    q: "3. Which model generates the prediction?",
    a: "Predictions are generated by a KNN Regressor (k=9, uniform weighting, Euclidean distance) built with scikit-learn. It was selected as the project's primary model because it captured non-linear relationships better than standard linear and Ridge regression baselines on this dataset."
  },
  {
    q: "4. How was the model evaluated?",
    a: "The dataset was split into an 80% training set (1,069 samples) and a 20% independent test set (268 samples). Models were also evaluated using 5-fold cross-validation on the training set to check for stability and guard against overfitting."
  },
  {
    q: "5. What does R² mean?",
    a: "R² (Coefficient of Determination) measures how well the model explains variation in annual medical costs on the evaluation data. A test R² of 0.8185 means approximately 81.8% of the variation in target charges in the test set is accounted for by the model."
  },
  {
    q: "6. What does MAE mean?",
    a: "MAE (Mean Absolute Error) measures the average absolute difference between predicted and actual cost. On our held-out test data, the model's typical error is $3,632.17."
  },
  {
    q: "7. Is my input stored?",
    a: "The application does not intentionally store prediction inputs in a database. Data submitted via the prediction form is processed in memory to compute the estimate and returned to your browser."
  },
  {
    q: "8. Is this an official insurance quote?",
    a: "No. This is a machine-learning estimate based on patterns in the evaluation/training dataset. It is not an official insurance quote, underwriting decision, or financial contract from any insurer."
  },
  {
    q: "9. Can this model be used for medical decisions?",
    a: "No. This tool is designed strictly for statistical cost estimation and does not provide medical advice, diagnosis, treatment guidance, or clinical risk assessment."
  },
  {
    q: "10. What are the limitations of the dataset?",
    a: "The dataset contains 1,338 historical records with only six features. It lacks critical health factors used in real-world underwriting, such as pre-existing medical conditions, prescription medications, family health history, specific insurance plan tiers, deductibles, copayments, and medical cost inflation."
  }
];

export default function Methodology() {
  const [openIndex, setOpenIndex] = useState(0);

  return (
    <section id="faq" className="py-16 bg-slate-50 border-b border-slate-200">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        {/* Section Header */}
        <div className="max-w-3xl mx-auto text-center mb-12">
          <div className="inline-flex items-center space-x-1.5 px-3 py-1 rounded-full bg-blue-100 text-blue-800 text-xs font-semibold uppercase tracking-wider mb-3">
            <HelpCircle className="w-3.5 h-3.5 text-blue-600" />
            <span>Transparency & FAQ</span>
          </div>
          <h2 className="text-3xl font-extrabold text-slate-900 tracking-tight">
            Frequently Asked Questions & Model Limitations
          </h2>
          <p className="text-slate-600 mt-2 text-sm sm:text-base">
            Clear, transparent documentation on how MedCost.AI operates, how it was evaluated, and its practical boundaries.
          </p>
        </div>

        {/* Project Limitations & Boundaries */}
        <div className="grid grid-cols-1 md:grid-cols-3 gap-6 mb-12">
          {PROJECT_BOUNDARIES.map((item, idx) => {
            const Icon = item.icon;
            return (
              <div key={idx} className="p-6 rounded-2xl bg-white border border-slate-200 shadow-2xs">
                <div className="w-10 h-10 rounded-xl bg-blue-50 border border-blue-100 flex items-center justify-center text-blue-600 mb-4">
                  <Icon className="w-5 h-5" />
                </div>
                <h3 className="font-bold text-slate-900 text-base mb-1.5">{item.title}</h3>
                <p className="text-xs sm:text-sm text-slate-600 leading-relaxed">{item.desc}</p>
              </div>
            );
          })}
        </div>

        {/* FAQ Accordion */}
        <div className="max-w-3xl mx-auto">
          <h3 className="text-xl font-bold text-slate-900 mb-6 text-center sm:text-left">
            Practical Questions & Answers
          </h3>

          <div className="space-y-3.5">
            {FAQS.map((item, idx) => {
              const isOpen = openIndex === idx;
              return (
                <div
                  key={idx}
                  className="bg-white rounded-xl border border-slate-200 overflow-hidden shadow-2xs transition-all"
                >
                  <button
                    type="button"
                    onClick={() => setOpenIndex(isOpen ? -1 : idx)}
                    className="w-full p-4 sm:p-5 text-left flex items-center justify-between font-semibold text-slate-900 text-sm sm:text-base hover:bg-slate-50 transition-colors"
                  >
                    <span>{item.q}</span>
                    {isOpen ? (
                      <ChevronUp className="w-5 h-5 text-blue-600 flex-shrink-0 ml-2" />
                    ) : (
                      <ChevronDown className="w-5 h-5 text-slate-400 flex-shrink-0 ml-2" />
                    )}
                  </button>
                  {isOpen && (
                    <div className="px-4 sm:px-5 pb-5 pt-1 text-xs sm:text-sm text-slate-600 leading-relaxed border-t border-slate-100">
                      {item.a}
                    </div>
                  )}
                </div>
              );
            })}
          </div>
        </div>
      </div>
    </section>
  );
}
